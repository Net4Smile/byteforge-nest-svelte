overwrite: true
schema: "../backend/**/*.gql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      strictScalars: true
      useTypeImports: true
